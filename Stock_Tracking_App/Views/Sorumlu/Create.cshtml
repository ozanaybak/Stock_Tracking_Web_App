@model stockProject.Models.SORUMLU

@{
    Layout = "~/Views/Shared/_LayoutPageMain.cshtml";
    ViewBag.SekmeAdi = "Sorumlu Tanımlama";
    ViewBag.SayfaAdi = "Sorumlu Tanımlama";
    ViewBag.CardAdi = "Sorumlu Bilgileri";
}

<h2>@ViewBag.SayfaAdi</h2>
<hr />

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="card ">
        <h5 class="card-header h5 primary-color">@ViewBag.CardAdi</h5>
        <div class="card-body">
            <div class="form-horizontal">
                <div class="col-lg-6">

                    <div class="form-group">
                        <div class="md-form md-outline">
                            @Html.LabelFor(model => model.SORUMLU_ADI, htmlAttributes: new { @class = "" })
                            @Html.EditorFor(model => model.SORUMLU_ADI, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.SORUMLU_ADI, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.ActionLink("Vazgeç", "Index", "Home", null, new { @class = "btn btn-info btn-rounded" })
                        <input type="submit" value="Kaydet" class="btn btn-success btn-rounded" />
                    </div>

                </div>
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

